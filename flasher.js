import { ESPLoader, Transport } from 'https://cdn.jsdelivr.net/npm/esptool-js@0.5.6/+esm';
const $=s=>document.querySelector(s),fileInput=$('#flashFile'),button=$('#serialFlash'),choosePort=$('#choosePort'),log=$('#flashLog'),state=$('#deviceState'),next=$('#nextBoard'),erase=$('#eraseProgress'),write=$('#writeProgress');
let selectedFile=null;const formatSize=n=>`${(n/1024/1024).toFixed(2)} MB`;const showFile=f=>{selectedFile=f;$('#fileName').textContent=f.name;$('#fileMeta').textContent=`${formatSize(f.size)} · ready to flash`;};fileInput.onchange=()=>{if(fileInput.files[0])showFile(fileInput.files[0]);};$('#chooseAnother').onclick=()=>fileInput.click();(async()=>{try{const r=await fetch('firmware/airport-display.bin',{cache:'no-store'});if(!r.ok)throw Error('not found');showFile(new File([await r.arrayBuffer()],'airport-display.bin',{type:'application/octet-stream'}));}catch{$('#fileName').textContent='No bundled firmware found';$('#fileMeta').textContent='Choose a .bin file manually.';}})();
const say=(m)=>log.textContent=m;const pct=(bar,label,n)=>{bar.value=n;$(label).textContent=`${n}%`};
let port=null,transport=null,loader=null;
const selectPort=async()=>{if(!('serial'in navigator)){say('Web Serial is unavailable. Use Chrome or Edge.');return false}try{port=await navigator.serial.requestPort();const info=port.getInfo();const usb=info.usbVendorId?`USB ${info.usbVendorId.toString(16)}:${info.usbProductId?.toString(16)||'?'}`:'serial';state.textContent=`${usb} selected · ready to flash`;say('Port selected. Put the board in download mode, then click Flash firmware.');choosePort.textContent='USB port selected';return true}catch(e){state.textContent='Port selection cancelled';say(`Port selection failed: ${e.message||e}`);return false}};
choosePort.onclick=async()=>{next.hidden=true;if(port){state.textContent='USB port already selected · ready';say('Port is already selected. Put the board in download mode, then click Flash firmware.');return}choosePort.disabled=true;await selectPort();choosePort.disabled=false};
button.onclick=async()=>{const file=selectedFile;if(!file){say('Choose the merged firmware .bin first.');return}if(!port){say('Choose the USB port first.');return}button.disabled=true;choosePort.disabled=true;next.hidden=true;erase.value=write.value=0;$('#erasePercent').textContent=$('#writePercent').textContent='—';try{transport=new Transport(port,true);const terminal={clean(){},write(v){say(String(v))},writeLine(v){say(String(v))}};loader=new ESPLoader({transport,baudrate:115200,terminal});state.textContent='Connecting to ESP32-S3 bootloader';say('Resetting and synchronizing with the bootloader…');const chip=await loader.main();state.textContent=`${chip} detected · erasing`;say('Erasing the entire flash…');await loader.eraseFlash();pct(erase,'#erasePercent',100);say('Erase complete. Writing firmware…');const bytes=new Uint8Array(await file.arrayBuffer());let data='';const chunk=0x8000;for(let i=0;i<bytes.length;i+=chunk)data+=String.fromCharCode(...bytes.subarray(i,Math.min(i+chunk,bytes.length)));await loader.writeFlash({fileArray:[{address:0,data}],flashSize:'keep',flashMode:'keep',flashFreq:'keep',eraseAll:false,compress:true,reportProgress:(_,written,total)=>{const n=Math.round(written/total*100);pct(write,'#writePercent',n);say(`Writing firmware… ${n}%`)}});pct(write,'#writePercent',100);say('Flash complete. Resetting board…');await transport.disconnect();await port.close();loader=transport=port=null;state.textContent='Flash complete · board reset';next.hidden=false}catch(e){state.textContent='Flash failed';const msg=String(e.message||e);say(msg.includes('No serial data')?'No bootloader response. Hold BOOT, press/release RESET, release BOOT, then click Flash firmware again.':`Flash failed: ${msg}`);try{if(transport)await transport.disconnect();if(port&&port.readable)await port.close()}catch{}}finally{button.disabled=false;choosePort.disabled=false}};
